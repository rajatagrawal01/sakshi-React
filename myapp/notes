<!-- <div width="100px">
    <div left></div>
    <div right> </div>
</div> -->

<!-- div -->
<li><div class="toolsBtn" data-cmd="cmdLS">
									<div class="toolBtnNormal" data-type="button">
										Line Details
									</div>
								</div></li>
How to get this button



<!-- UI -->
                                <px:PXJavaScript runat="server" ID="CstJavaScript1" Script="
function customizeLineDetailGrid() {
    const button = document.getElementsByClassName('toolbtnnormal');
 
    if (!button) return;
 
    button.addEventListener('click', function () {
        setTimeout(function () {
            const parent = document.querySelector('#ctl00_phG_PanelLS_edewqclotserialstatus')?.parentElement;
            const grid = document.querySelector('#ctl00_phG_PanelLS_grid2');
 
            if (!parent || !grid) return;
 
            grid.style.width =
                getComputedStyle(parent).display === 'none' ? '930px' : '500px';
        }, 500);
    });
}
 
 
customizeLineDetailGrid();
" />



if button is clicked left should cover whole width